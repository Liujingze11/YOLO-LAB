# i18n Multi-Language Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add runtime-switchable multi-language support (zh/en/fr/es) to GUI widgets and subprocess terminal output.

**Architecture:** JSON translation files per language. `i18n.py` manager loads JSON, exposes `tr(key)` and `apply_language(root)`. Widgets get `i18nKey` dynamic property. Language switcher QComboBox in corner widget area. Subprocesses receive `--lang` argument.

**Tech Stack:** Python 3.10, PySide6, JSON

---

### Task 1: Create translation JSON files

**Files:**
- Create: `gui/locales/zh.json`
- Create: `gui/locales/en.json`
- Create: `gui/locales/fr.json`
- Create: `gui/locales/es.json`

- [ ] **Step 1: Create gui/locales/ directory and zh.json**

```bash
mkdir -p gui/locales
```

```json
{
  "app.title": "YOLO Lab",
  "tab.train": "训练",
  "tab.infer": "推理",
  "tab.logs": "日志 & 结果",
  "tab.tools": "工具",

  "train.card.paths": "路径",
  "train.data_yaml": "data.yaml",
  "train.field.results_dir": "结果目录",
  "train.field.log_dir": "日志目录",
  "train.field.init_weights": "初始权重",
  "train.btn.scan": "扫描模型",
  "train.btn.edit_yaml": "编辑 data.yaml",
  "train.btn.browse": "浏览",
  "train.card.hyperparams": "超参数",
  "train.field.epochs": "Epochs",
  "train.field.imgsz": "Imgsz",
  "train.field.batch": "Batch",
  "train.field.device": "Device",
  "train.field.exp_name": "实验名称",
  "train.card.mode": "训练模式",
  "train.rb.new": "新训练 — 从初始权重开始",
  "train.rb.resume": "续训 — 从上一次 last.pt 继续",
  "train.rb.finetune": "微调 — 基于历史实验的 best.pt",
  "train.field.history": "历史实验",
  "train.btn.refresh": "刷新",
  "train.augment": "启用数据增强",
  "train.btn.start": "开始训练",
  "train.btn.stop": "停止训练",
  "train.btn.continue": "继续训练",
  "train.btn.end": "结束训练",
  "train.btn.reset": "恢复默认",
  "train.combo.presets": "— 预设 —",
  "train.btn.save_preset": "保存预设",
  "train.btn.delete_preset": "删除预设",
  "train.progress.format": "Epoch %v / %m",
  "train.log.output": "输出",

  "model.group.v8_seg": "YOLOv8 分割",
  "model.group.v11_seg": "YOLO11 分割",
  "model.group.v12_seg": "YOLO12 分割",
  "model.group.v8_det": "YOLOv8 检测",
  "model.group.v11_det": "YOLO11 检测",
  "model.group.local": "本地文件",
  "model.action.browse": "浏览本地文件...",
  "model.tag.download": "[点击下载]",
  "model.tag.downloading": "(下载中...)",
  "model.dialog.title": "下载模型",
  "model.dialog.downloading": "正在下载 {filename} ...",
  "model.dialog.failed": "下载失败: {filename}",
  "model.dialog.cancelled": "已取消下载",
  "model.dialog.cancel_btn": "取消下载",
  "model.dialog.close_btn": "关闭",

  "infer.card.config": "推理配置",
  "infer.field.model": "模型 .pt",
  "infer.field.source": "输入源",
  "infer.field.save": "保存目录",
  "infer.field.conf": "Conf",
  "infer.field.imgsz": "Imgsz",
  "infer.btn.start": "开始推理",
  "infer.btn.stop": "停止推理",
  "infer.progress.format": "Image %v / %m",
  "infer.log.output": "输出",

  "tools.card.dataset": "数据集目录",
  "tools.card.tools": "工具",
  "tools.btn.run": "执行工具",
  "tools.btn.stop": "停止",
  "tools.btn.browse": "浏览",
  "tools.log.output": "输出",

  "logs.card.logdir": "日志目录",
  "logs.card.files": "历史日志文件",
  "logs.combo.csv_placeholder": "— 选择 CSV 文件 —",
  "logs.card.experiments": "实验 & 结果",
  "logs.field.experiment": "实验",
  "logs.combo.exp_placeholder": "— 选择实验 —",
  "logs.btn.exp_dir": "打开实验目录",
  "logs.btn.weights": "打开权重目录",
  "logs.btn.plot": "查看训练图表",
  "logs.card.quick": "快捷目录",
  "logs.btn.results": "训练结果",
  "logs.btn.predictions": "推理结果",
  "logs.btn.dataset": "数据集",

  "tools.item.empty_labels": "创建空标签文件",
  "tools.item.random_train_val": "随机分割 train/val（含标签）",
  "tools.item.random_train_val_test": "随机分割 train/val/test（含标签）",
  "tools.item.every_nth_labels": "每 N 张分割 train/val（含标签）",
  "tools.item.random_images_only": "随机分割 train/val（仅图片）",
  "tools.item.every_nth_images_only": "每 N 张分割 train/val（仅图片）",
  "tools.param.val_ratio": "验证集比例 %",
  "tools.param.test_ratio": "测试集比例 %",
  "tools.param.interval": "间隔 N (每 N 张取 1 张)",

  "msg.title.confirm": "确认训练",
  "msg.title.hint": "提示",
  "msg.title.error": "错误",
  "msg.title.done": "完成",
  "msg.title.failed": "训练失败",
  "msg.title.infer_failed": "推理失败",
  "msg.title.tool_failed": "工具失败",
  "msg.train_running": "训练正在进行中。",
  "msg.infer_running": "推理正在进行中。",
  "msg.tool_running": "工具正在执行中，请等待完成或先停止。",
  "msg.train_done": "训练流程已结束。",
  "msg.infer_done": "推理已结束。",
  "msg.tool_done": "工具执行已结束。",
  "msg.select_dataset": "请选择数据集目录。",
  "msg.dataset_not_found": "数据集目录不存在：",
  "msg.tool_not_found": "工具脚本不存在：",
  "msg.no_model": "找不到初始权重：",
  "msg.no_best_pt": "找不到：",
  "msg.no_last_pt": "未找到续训权重：",
  "msg.no_best_for_resume": "未找到续训权重",
  "msg.fallback_new_train": "是否改为模式 1 新训练？",
  "msg.no_history_selected": "请在「历史实验」下拉框中选择一项。",
  "msg.select_experiment": "请先选择实验。",
  "msg.infer_model_not_found": "找不到模型：",
  "msg.infer_source_not_found": "找不到输入：",
  "msg.invalid_conf": "Conf 值无效:",
  "msg.weights_dir_not_found": "权重目录不存在：",
  "msg.plot_not_found": "图表不存在，将打开实验目录。",
  "msg.yaml_not_found": "文件不存在：",
  "msg.cannot_open_file": "无法打开文件，请手动打开。",
  "msg.csv_read_failed": "读取失败:",
  "msg.log_empty": "日志为空",
  "msg.log_loaded": "加载:",
  "msg.log_rows": "行",
  "msg.process_failed": "进程退出码:",

  "log.info": "info",
  "log.ok": "ok",
  "log.warn": "warn",
  "log.err": "err!",
  "log.info_prefix": "[info]",
  "log.ok_prefix": "[ ok ]",
  "log.warn_prefix": "[warn]",
  "log.err_prefix": "[err!]",

  "train.log.scan_found": "找到 {count} 个已训练模型",
  "train.log.scan_none": "未找到已训练模型",
  "train.log.defaults_reset": "已恢复默认配置",
  "train.log.preset_loaded": "已加载预设：「{name}」",
  "train.log.preset_saved": "预设已保存：「{name}」",
  "train.log.preset_deleted": "已删除预设：「{name}」",

  "train.engine.new_start": "开始新训练 — 实验: {name}",
  "train.engine.new_weights": "权重: {model}  |  epochs={epochs}  imgsz={imgsz}  batch={batch}  device={device}",
  "train.engine.augment_on": "数据增强: 开启",
  "train.engine.augment_off": "数据增强: 关闭",
  "train.engine.resume_fallback": "未找到续训权重，改为新训练: {path}",
  "train.engine.resume_start": "继续训练 — 实验: {name}",
  "train.engine.resume_weights": "权重: {path}",
  "train.engine.no_best_pt": "未找到 best.pt，无法记录验证结果：{path}",
  "train.engine.val_logged": "验证结果已记录到日志。",
  "train.engine.val_failed": "记录验证结果失败：{err}",
  "train.engine.no_mode": "错误：无交互模式必须指定 --mode (1/2/3)",
  "train.engine.fix_path": "已修正数据集路径: {old} -> {new}",
  "train.engine.log_started": "开始新训练，数据增强={aug}",
  "train.engine.log_finished": "训练完成，数据增强={aug}",
  "train.engine.log_val": "训练完成后的验证结果",
  "train.engine.log_resume_started": "继续上次训练",
  "train.engine.log_resume_finished": "继续训练完成",
  "train.engine.log_resume_val": "继续训练后的验证结果",
  "train.engine.log_finetune_started": "基于历史实验 {exp} 开始训练，数据增强={aug}",
  "train.engine.log_finetune_finished": "基于历史实验 {exp} 的训练完成，数据增强={aug}",
  "train.engine.log_finetune_val": "基于历史实验 {exp} 的验证结果",

  "train.msg.confirm_summary": "{details}\n{'─' * 40}\nEpochs:  {epochs:<6} Imgsz: {imgsz}\nBatch:   {batch:<6} Device: {device}\n数据增强: {aug}\n{'─' * 40}\n是否开始训练？",
  "train.msg.new_summary": "模式:  {mode}\n实验:  {exp}\n权重:  {weights}\n数据:  {data}",
  "train.msg.resume_summary": "模式:  {mode}\n实验:  {exp}\n权重:  {weights}\n数据:  {data}",
  "train.msg.finetune_summary": "模式:  {mode}\n实验:  {exp}\n基础:  {base}\n权重:  {weights}\n数据:  {data}",
  "train.msg.mode_new": "新训练",
  "train.msg.mode_new_fallback": "新训练（降级）",
  "train.msg.mode_resume": "续训",
  "train.msg.mode_finetune": "微调",

  "train.log.starting": "开始训练 — {name}",
  "train.log.params": "epochs={epochs}  imgsz={imgsz}  batch={batch}  device={device}",
  "train.log.stopping": "正在停止训练...",
  "train.log.paused": "训练已暂停 — 可点击「继续训练」恢复，或点击「结束训练」终止本次会话",
  "train.log.ended": "本次训练会话已结束",
  "train.log.exiting": "正在退出，终止训练/推理进程...",
  "train.log.tool_stopping": "正在停止...",
  "train.log.tool_stopped": "工具已停止",

  "infer.engine.model": "模型: {path}",
  "infer.engine.task": "自动识别任务: {task}",
  "infer.engine.source": "输入源: {source}",
  "infer.engine.output": "输出目录: {dir}",
  "infer.engine.done": "推理完成，共保存 {count} 张结果到: {dir}",

  "infer.log.starting": "开始推理 — {model}",
  "infer.log.stopping": "正在停止推理...",
  "infer.log.stopped": "推理已停止",
  "infer.eta": "已耗时 {elapsed:.0f}s  |  预计剩余 {eta:.0f}s  |  共 {total} 张",

  "tool.log.running": "执行: {script}",
  "tool.log.done": "工具执行完成",
  "tool.log.failed": "工具执行失败",
  "tool.log.cancelled": "工具已停止",

  "worker.process_failed": "启动进程失败: {err}"
}
```

- [ ] **Step 2: Create en.json**

```json
{
  "app.title": "YOLO Lab",
  "tab.train": "Train",
  "tab.infer": "Inference",
  "tab.logs": "Logs & Results",
  "tab.tools": "Tools",

  "train.card.paths": "Paths",
  "train.data_yaml": "data.yaml",
  "train.field.results_dir": "Results Dir",
  "train.field.log_dir": "Log Dir",
  "train.field.init_weights": "Init Weights",
  "train.btn.scan": "Scan Models",
  "train.btn.edit_yaml": "Edit data.yaml",
  "train.btn.browse": "Browse",
  "train.card.hyperparams": "Hyperparameters",
  "train.field.epochs": "Epochs",
  "train.field.imgsz": "Imgsz",
  "train.field.batch": "Batch",
  "train.field.device": "Device",
  "train.field.exp_name": "Experiment Name",
  "train.card.mode": "Training Mode",
  "train.rb.new": "New — From initial weights",
  "train.rb.resume": "Resume — From last.pt",
  "train.rb.finetune": "Fine-tune — From experiment best.pt",
  "train.field.history": "History",
  "train.btn.refresh": "Refresh",
  "train.augment": "Enable Data Augmentation",
  "train.btn.start": "Start Training",
  "train.btn.stop": "Stop Training",
  "train.btn.continue": "Continue Training",
  "train.btn.end": "End Training",
  "train.btn.reset": "Reset Defaults",
  "train.combo.presets": "— Preset —",
  "train.btn.save_preset": "Save Preset",
  "train.btn.delete_preset": "Delete Preset",
  "train.progress.format": "Epoch %v / %m",
  "train.log.output": "Output",

  "model.group.v8_seg": "YOLOv8 Segmentation",
  "model.group.v11_seg": "YOLO11 Segmentation",
  "model.group.v12_seg": "YOLO12 Segmentation",
  "model.group.v8_det": "YOLOv8 Detection",
  "model.group.v11_det": "YOLO11 Detection",
  "model.group.local": "Local Files",
  "model.action.browse": "Browse local file...",
  "model.tag.download": "[Click to download]",
  "model.tag.downloading": "(Downloading...)",
  "model.dialog.title": "Download Model",
  "model.dialog.downloading": "Downloading {filename} ...",
  "model.dialog.failed": "Download failed: {filename}",
  "model.dialog.cancelled": "Download cancelled",
  "model.dialog.cancel_btn": "Cancel Download",
  "model.dialog.close_btn": "Close",

  "infer.card.config": "Inference Config",
  "infer.field.model": "Model .pt",
  "infer.field.source": "Source",
  "infer.field.save": "Save Dir",
  "infer.field.conf": "Conf",
  "infer.field.imgsz": "Imgsz",
  "infer.btn.start": "Start Inference",
  "infer.btn.stop": "Stop Inference",
  "infer.progress.format": "Image %v / %m",
  "infer.log.output": "Output",

  "tools.card.dataset": "Dataset Directory",
  "tools.card.tools": "Tools",
  "tools.btn.run": "Run Tool",
  "tools.btn.stop": "Stop",
  "tools.btn.browse": "Browse",
  "tools.log.output": "Output",

  "logs.card.logdir": "Log Directory",
  "logs.card.files": "History Logs",
  "logs.combo.csv_placeholder": "— Select CSV —",
  "logs.card.experiments": "Experiments & Results",
  "logs.field.experiment": "Experiment",
  "logs.combo.exp_placeholder": "— Select Experiment —",
  "logs.btn.exp_dir": "Open Exp Dir",
  "logs.btn.weights": "Open Weights",
  "logs.btn.plot": "View Training Plot",
  "logs.card.quick": "Quick Access",
  "logs.btn.results": "Training Results",
  "logs.btn.predictions": "Inference Results",
  "logs.btn.dataset": "Dataset",

  "tools.item.empty_labels": "Create Empty Labels",
  "tools.item.random_train_val": "Random Split train/val (with labels)",
  "tools.item.random_train_val_test": "Random Split train/val/test (with labels)",
  "tools.item.every_nth_labels": "Every N Split train/val (with labels)",
  "tools.item.random_images_only": "Random Split train/val (images only)",
  "tools.item.every_nth_images_only": "Every N Split train/val (images only)",
  "tools.param.val_ratio": "Validation Ratio %",
  "tools.param.test_ratio": "Test Ratio %",
  "tools.param.interval": "Interval N (1 per N)",

  "msg.title.confirm": "Confirm Training",
  "msg.title.hint": "Notice",
  "msg.title.error": "Error",
  "msg.title.done": "Complete",
  "msg.title.failed": "Training Failed",
  "msg.title.infer_failed": "Inference Failed",
  "msg.title.tool_failed": "Tool Failed",
  "msg.train_running": "Training is in progress.",
  "msg.infer_running": "Inference is in progress.",
  "msg.tool_running": "Tool is running. Please wait or stop first.",
  "msg.train_done": "Training completed.",
  "msg.infer_done": "Inference completed.",
  "msg.tool_done": "Tool execution completed.",
  "msg.select_dataset": "Please select a dataset directory.",
  "msg.dataset_not_found": "Dataset directory not found:",
  "msg.tool_not_found": "Tool script not found:",
  "msg.no_model": "Initial weights not found:",
  "msg.no_best_pt": "Not found:",
  "msg.no_last_pt": "Resume weights not found:",
  "msg.no_best_for_resume": "Resume weights not found",
  "msg.fallback_new_train": "Switch to mode 1 (new training)?",
  "msg.no_history_selected": "Please select a history experiment.",
  "msg.select_experiment": "Please select an experiment.",
  "msg.infer_model_not_found": "Model not found:",
  "msg.infer_source_not_found": "Source not found:",
  "msg.invalid_conf": "Invalid Conf value:",
  "msg.weights_dir_not_found": "Weights directory not found:",
  "msg.plot_not_found": "Plot not found. Opening experiment directory.",
  "msg.yaml_not_found": "File not found:",
  "msg.cannot_open_file": "Cannot open file. Please open manually.",
  "msg.csv_read_failed": "Read failed:",
  "msg.log_empty": "Log is empty",
  "msg.log_loaded": "Loaded:",
  "msg.log_rows": "rows",
  "msg.process_failed": "Process exit code:",

  "log.info": "info",
  "log.ok": "ok",
  "log.warn": "warn",
  "log.err": "err!",
  "log.info_prefix": "[info]",
  "log.ok_prefix": "[ ok ]",
  "log.warn_prefix": "[warn]",
  "log.err_prefix": "[err!]",

  "train.log.scan_found": "Found {count} trained model(s)",
  "train.log.scan_none": "No trained models found",
  "train.log.defaults_reset": "Defaults restored",
  "train.log.preset_loaded": "Preset loaded: \"{name}\"",
  "train.log.preset_saved": "Preset saved: \"{name}\"",
  "train.log.preset_deleted": "Preset deleted: \"{name}\"",

  "train.engine.new_start": "Starting new training — Experiment: {name}",
  "train.engine.new_weights": "Weights: {model}  |  epochs={epochs}  imgsz={imgsz}  batch={batch}  device={device}",
  "train.engine.augment_on": "Augmentation: ON",
  "train.engine.augment_off": "Augmentation: OFF",
  "train.engine.resume_fallback": "Resume weights not found, falling back to new training: {path}",
  "train.engine.resume_start": "Resuming training — Experiment: {name}",
  "train.engine.resume_weights": "Weights: {path}",
  "train.engine.no_best_pt": "best.pt not found, cannot log validation: {path}",
  "train.engine.val_logged": "Validation result logged.",
  "train.engine.val_failed": "Failed to log validation: {err}",
  "train.engine.no_mode": "Error: --mode (1/2/3) required in non-interactive mode",
  "train.engine.fix_path": "Fixed dataset path: {old} -> {new}",
  "train.engine.log_started": "New training started, augmentation={aug}",
  "train.engine.log_finished": "Training finished, augmentation={aug}",
  "train.engine.log_val": "Validation result after training",
  "train.engine.log_resume_started": "Resuming previous training",
  "train.engine.log_resume_finished": "Resumed training finished",
  "train.engine.log_resume_val": "Validation result after resuming",
  "train.engine.log_finetune_started": "Fine-tuning based on {exp}, augmentation={aug}",
  "train.engine.log_finetune_finished": "Fine-tuning based on {exp} finished, augmentation={aug}",
  "train.engine.log_finetune_val": "Validation result from fine-tuning {exp}",

  "train.msg.confirm_summary": "{details}\n{'─' * 40}\nEpochs:  {epochs:<6} Imgsz: {imgsz}\nBatch:   {batch:<6} Device: {device}\nAugmentation: {aug}\n{'─' * 40}\nStart training?",
  "train.msg.new_summary": "Mode:  {mode}\nExperiment:  {exp}\nWeights:  {weights}\nData:  {data}",
  "train.msg.resume_summary": "Mode:  {mode}\nExperiment:  {exp}\nWeights:  {weights}\nData:  {data}",
  "train.msg.finetune_summary": "Mode:  {mode}\nExperiment:  {exp}\nBase:  {base}\nWeights:  {weights}\nData:  {data}",
  "train.msg.mode_new": "New Training",
  "train.msg.mode_new_fallback": "New Training (fallback)",
  "train.msg.mode_resume": "Resume",
  "train.msg.mode_finetune": "Fine-tune",

  "train.log.starting": "Starting training — {name}",
  "train.log.params": "epochs={epochs}  imgsz={imgsz}  batch={batch}  device={device}",
  "train.log.stopping": "Stopping training...",
  "train.log.paused": "Training paused — click Continue to resume, or End to finish this session",
  "train.log.ended": "Training session ended",
  "train.log.exiting": "Exiting, stopping training/inference processes...",
  "train.log.tool_stopping": "Stopping...",
  "train.log.tool_stopped": "Tool stopped",

  "infer.engine.model": "Model: {path}",
  "infer.engine.task": "Auto-detected task: {task}",
  "infer.engine.source": "Source: {source}",
  "infer.engine.output": "Output dir: {dir}",
  "infer.engine.done": "Inference done, saved {count} images to: {dir}",

  "infer.log.starting": "Starting inference — {model}",
  "infer.log.stopping": "Stopping inference...",
  "infer.log.stopped": "Inference stopped",
  "infer.eta": "Elapsed {elapsed:.0f}s  |  ETA {eta:.0f}s  |  Total {total} images",

  "tool.log.running": "Running: {script}",
  "tool.log.done": "Tool execution complete",
  "tool.log.failed": "Tool execution failed",
  "tool.log.cancelled": "Tool stopped",

  "worker.process_failed": "Failed to start process: {err}"
}
```

- [ ] **Step 3: Create fr.json** (French)

Same keys, French values. Key translations:
  - tab.train: "Entraînement", tab.infer: "Inférence", tab.logs: "Journaux & Résultats", tab.tools: "Outils"
  - train.btn.start: "Démarrer", train.btn.stop: "Arrêter", train.btn.browse: "Parcourir"
  - model.dialog.cancel_btn: "Annuler", model.dialog.close_btn: "Fermer"
  - Full file with all keys written out.

- [ ] **Step 4: Create es.json** (Spanish)

Same keys, Spanish values. Key translations:
  - tab.train: "Entrenamiento", tab.infer: "Inferencia", tab.logs: "Registros y Resultados", tab.tools: "Herramientas"
  - train.btn.start: "Iniciar", train.btn.stop: "Detener", train.btn.browse: "Examinar"
  - model.dialog.cancel_btn: "Cancelar", model.dialog.close_btn: "Cerrar"
  - Full file with all keys written out.

---

### Task 2: Create i18n.py translation manager

**Files:**
- Create: `gui/gui/i18n.py`

- [ ] **Step 1: Write i18n.py**

```python
"""
i18n multi-language translation manager.
"""
from __future__ import annotations

import json
from pathlib import Path

from PySide6.QtCore import QObject, Signal
from PySide6.QtWidgets import (
    QCheckBox,
    QComboBox,
    QLabel,
    QPushButton,
    QRadioButton,
    QTabWidget,
    QWidget,
)

LOCALE_DIR = Path(__file__).resolve().parent.parent / "locales"
AVAILABLE_LANGS = {"zh": "中文", "en": "English", "fr": "Français", "es": "Español"}


class I18nManager(QObject):
    language_changed = Signal(str)  # lang code

    def __init__(self):
        super().__init__()
        self._current_lang = "zh"
        self._translations: dict[str, str] = {}
        self._load_locale("zh")

    @property
    def current_lang(self) -> str:
        return self._current_lang

    def tr(self, key: str, **kwargs) -> str:
        text = self._translations.get(key, key)
        if kwargs:
            text = text.format(**kwargs)
        return text

    def set_language(self, lang: str) -> None:
        if lang == self._current_lang:
            return
        self._load_locale(lang)
        self.language_changed.emit(lang)

    def apply_language(self, root: QWidget) -> None:
        """Recursively update all widgets with i18nKey property."""
        stack = [root]
        while stack:
            w = stack.pop()
            key = w.property("i18nKey")
            if key:
                self._update_widget(w, key)
            for child in w.children():
                if isinstance(child, QWidget):
                    stack.append(child)

    def _update_widget(self, w: QWidget, base_key: str) -> None:
        if isinstance(w, QLabel):
            w.setText(self.tr(base_key))
        elif isinstance(w, (QPushButton, QCheckBox, QRadioButton)):
            w.setText(self.tr(base_key))
        elif isinstance(w, QTabWidget):
            for i in range(w.count()):
                tab_key = w.tabBar().tabData(i)
                if tab_key:
                    w.setTabText(i, self.tr(tab_key))
        elif isinstance(w, QComboBox):
            for i in range(w.count()):
                item_key = w.itemData(i, 0x0100)  # Qt.UserRole + 1
                if item_key:
                    w.setItemText(i, self.tr(item_key))

    def _load_locale(self, lang: str) -> None:
        path = LOCALE_DIR / f"{lang}.json"
        with open(path, "r", encoding="utf-8") as f:
            self._translations = json.load(f)
        self._current_lang = lang


_i18n = I18nManager()


def tr(key: str, **kwargs) -> str:
    return _i18n.tr(key, **kwargs)


def set_language(lang: str) -> None:
    _i18n.set_language(lang)


def apply_language(root: QWidget) -> None:
    _i18n.apply_language(root)


def current_lang() -> str:
    return _i18n.current_lang


def language_changed_signal() -> Signal:
    return _i18n.language_changed
```

---

### Task 3: Update widgets.py with i18nKey

**Files:**
- Modify: `gui/gui/widgets.py`

- [ ] **Step 1: Add optional i18n_key parameter to factory functions**

Modify each factory function signature to accept `i18n_key: str | None = None` and set the property:

```python
def progress_bar(parent: QWidget | None = None, i18n_key: str | None = None) -> QProgressBar:
    p = QProgressBar(parent)
    ...
    if i18n_key:
        p.setProperty("i18nKey", i18n_key)
    p.setStyleSheet(PROGRESS_STYLE)
    return p
```

Apply same pattern to: `card` (on the card widget), `section_label`, `field_label`, `input_`, `path_combo`, `spinner`, `btn`, `tiny_btn`, `danger_btn`, `log_area`, `scroll_area`, `simple_combo`.

For `card()`, set i18nKey on the returned card widget.
For `progress_bar()`, also use `tr()` for the format string.

- [ ] **Step 2: Update progress_bar format to use tr()**

```python
from gui.i18n import tr

def progress_bar(parent: QWidget | None = None, i18n_key: str | None = None) -> QProgressBar:
    ...
    p.setFormat(tr("train.progress.format"))
    ...
```

---

### Task 4: Update model_selector.py with i18n support

**Files:**
- Modify: `gui/gui/model_selector.py`

- [ ] **Step 1: Update _add_model_item and group labels to use tr()**

```python
from gui.i18n import tr

# In _populate(), replace hardcoded group labels:
_add(f"── {tr('model.group.v8_seg')} ──", None, enabled=False, color=COLOR_DISABLED)
_add(f"── {tr('model.group.v11_seg')} ──", None, enabled=False, color=COLOR_DISABLED)
# ... etc for all group labels

# In _add_model_item:
if state == "ok":
    _add_fn(f"{display}  ✓", fn, enabled=True)
elif state == "downloading":
    _add_fn(f"{display}  {tr('model.tag.downloading')}", fn, enabled=True, color=COLOR_DISABLED)
else:
    _add_fn(f"{display}  {tr('model.tag.download')}", fn, enabled=True, color=COLOR_DISABLED)

# "本地文件" → tr("model.group.local")
# "浏览本地文件..." → tr("model.action.browse")
```

- [ ] **Step 2: Update DownloadDialog to use tr()**

```python
class DownloadDialog(QDialog):
    def __init__(self, filename: str, parent=None):
        ...
        self.setWindowTitle(tr("model.dialog.title"))
        self._label = QLabel(tr("model.dialog.downloading", filename=filename))
        ...
        self._cancel_btn = danger_btn(tr("model.dialog.cancel_btn"))
        ...
    
    def set_error(self, msg):
        self._label.setText(tr("model.dialog.failed", filename=self._filename))
        ...
    
    def set_cancelled(self):
        self._label.setText(tr("model.dialog.cancelled"))
        ...
```

- [ ] **Step 3: Update _on_download_error to use tr("model.dialog.close_btn")**

---

### Task 5: Update main.py with language switcher

**Files:**
- Modify: `gui/gui/main.py`

- [ ] **Step 1: Add import and language combo to __init__**

```python
from gui.i18n import tr, set_language, apply_language, current_lang, AVAILABLE_LANGS

# In __init__, add language combo next to dark button:
lang_combo = QComboBox()
lang_combo.setProperty("themeClass", "combo")
lang_combo.setStyleSheet(COMBO_STYLE)
lang_combo.setMaximumWidth(90)
for code, name in AVAILABLE_LANGS.items():
    lang_combo.addItem(name, code)
lang_combo.setCurrentIndex(0)
lang_combo.currentIndexChanged.connect(self._on_lang_changed)

# Update corner container to hold both:
corner = QWidget()
cl = QHBoxLayout(corner)
cl.setContentsMargins(0, 0, 8, 0)
cl.setSpacing(6)
cl.addWidget(lang_combo)
cl.addWidget(self._dark_btn)
```

- [ ] **Step 2: Add _on_lang_changed method**

```python
def _on_lang_changed(self, idx):
    lang = self._tabs.cornerWidget().findChild(QComboBox).itemData(idx)
    if lang:
        set_language(lang)
        apply_language(self)
```

- [ ] **Step 3: Add i18nKey to all widgets in _build_train_tab**

For section labels:
```python
header1.addWidget(section_label(tr("train.card.paths")))
# → use i18n_key parameter:
header1.addWidget(section_label("train.card.paths"))
```

For field labels:
```python
lbl = field_label(tr("train.data_yaml"), i18n_key="train.data_yaml")
```

Wait, the cleaner approach: pass the key to the factory and use it both for the text and the property. Let me think about this more carefully.

Actually, the cleanest approach: each factory function that takes a `text` parameter should also accept an `i18n_key` parameter. If `i18n_key` is provided, use `tr(i18n_key)` for the initial text and set the property for later updates.

But this would require modifying all factory functions in Task 3 to accept `i18n_key` and call `tr()` internally.

Let me restructure: In Task 3, modify factory functions to accept `i18n_key` and automatically call `tr(i18n_key)` for the initial text. Then in Task 5 (main.py), just pass the key to the factory.

Let me rewrite the plan more concisely. Actually, this is getting very complex. Let me simplify.

**Simplified approach for Task 5:**

Instead of modifying every factory function, just set i18nKey AFTER creation:

```python
lbl = field_label(tr("train.data_yaml"))
lbl.setProperty("i18nKey", "train.data_yaml")
```

This keeps factory functions unchanged (except progress_bar which has a format string).

This is the approach I'll use in the plan.

- [ ] **Step 3: Set i18nKey on all widgets in main.py**

Pattern for each widget:
```python
# Before:
lbl = field_label("data.yaml")

# After:
lbl = field_label(tr("train.data_yaml"))
lbl.setProperty("i18nKey", "train.data_yaml")
```

Apply to ALL user-visible strings in all four tabs.

- [ ] **Step 4: Update QMessageBox calls to use tr()**

```python
QMessageBox.warning(self, tr("msg.title.hint"), tr("msg.train_running"))
QMessageBox.question(self, tr("msg.title.confirm"), summary, ...)
# etc.
```

- [ ] **Step 5: Update log helper methods to use tr()**

```python
def _log_info(self, msg):
    self._log_append(self.tr_log, f'<span style="color:#6ec6ff;">{tr("log.info_prefix")}</span>  {msg}')

def _log_good(self, msg):
    self._log_append(self.tr_log, f'<span style="color:#50fa7b;">{tr("log.ok_prefix")}</span>  {msg}')
# etc.
```

- [ ] **Step 6: Update tab names**

```python
self._tabs.addTab(self._build_train_tab(), tr("tab.train"))
self._tabs.addTab(self._build_infer_tab(), tr("tab.infer"))
self._tabs.addTab(self._build_log_viewer_tab(), tr("tab.logs"))
self._tabs.addTab(self._build_tools_tab(), tr("tab.tools"))

# Also set tabData for re-translation:
for i, key in enumerate(["tab.train", "tab.infer", "tab.logs", "tab.tools"]):
    self._tabs.tabBar().setTabData(i, key)
```

- [ ] **Step 7: Update dark toggle button init**

```python
self._dark_btn = QPushButton("☀")  # unchanged, emoji is universal
```

- [ ] **Step 8: Update train mode labels and confirm dialogs**

- [ ] **Step 9: Update tool tab strings (tool items, params)**

- [ ] **Step 10: Update _start_train to pass --lang to subprocess**

```python
cmd = [
    sys.executable, str(ROOT / "gui" / "train_engine.py"),
    "--lang", current_lang(),
    ...
]
```

- [ ] **Step 11: Update _start_infer to pass --lang**

```python
cmd = [
    sys.executable, str(ROOT / "gui" / "infer_engine.py"),
    "--lang", current_lang(),
    ...
]
```

---

### Task 6: Update train_engine.py for translated output

**Files:**
- Modify: `gui/gui/train_engine.py`

- [ ] **Step 1: Add argparse --lang and load locale**

```python
# In run_non_interactive or main:
import argparse
parser.add_argument("--lang", default="zh")

# Load translations:
from gui.i18n import tr, _load_locale_path
locale_path = ROOT.parent / "locales" / f"{args.lang}.json"
with open(locale_path) as f:
    _loc = json.load(f)
def _t(key, **kw):
    return _loc.get(key, key).format(**kw) if kw else _loc.get(key, key)
```

- [ ] **Step 2: Replace all print() strings with _t() calls**

```python
# Before:
print(f"开始新训练 — 实验: {config.experiment_name}")

# After:
print(_t("train.engine.new_start", name=config.experiment_name))
```

Apply to ALL print statements in the file.

---

### Task 7: Update infer_engine.py for translated output

**Files:**
- Modify: `gui/gui/infer_engine.py`

- [ ] **Step 1: Add --lang and load locale** (same pattern as Task 6)

- [ ] **Step 2: Replace all print() strings with _t() calls**

---

### Task 8: Final verification and commit

- [ ] **Step 1: Syntax check all modified files**

```bash
conda run -n yolo python3 -c "
import ast
for f in ['gui/gui/i18n.py', 'gui/gui/main.py', 'gui/gui/widgets.py', 'gui/gui/model_selector.py', 'gui/gui/train_engine.py', 'gui/gui/infer_engine.py']:
    ast.parse(open(f).read())
    print(f'{f}: OK')
"
```

- [ ] **Step 2: Import check**

```bash
conda run -n yolo python3 -c "
from gui.i18n import tr, set_language, apply_language, current_lang
from gui.main import MainWindow
print('All imports OK')
print('zh:', tr('tab.train'))
set_language('en')
print('en:', tr('tab.train'))
print('PASS')
"
```

- [ ] **Step 3: Commit**

```bash
git add gui/locales/ gui/gui/i18n.py gui/gui/widgets.py gui/gui/model_selector.py gui/gui/main.py gui/gui/train_engine.py gui/gui/infer_engine.py docs/superpowers/
git commit -m "feat: add i18n multi-language support (zh/en/fr/es)

- GUI widgets and subprocess output support runtime language switching
- JSON translation files for Chinese, English, French, Spanish
- Recursive widget tree update via i18nKey dynamic property
- Subprocesses receive --lang parameter for translated terminal output"
```
